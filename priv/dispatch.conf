{["bet", id], mybets_bet, []}.
{["bet"], mybets_bet, []}.

{["dev", "wmtrace", '*'], wmtrace_resource, [{trace_dir, "/tmp/traces"}]}.

% The canonical resource for everything else
{[], mybets_resource, []}.
